// Observer domain will be extracted here from apps/reader/src/observer
// as the text-centered canvas rewrite proceeds. For now the runnable code
// lives in the Reader app so Titus progress keeps working without a split.
export {};
